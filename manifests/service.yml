apiVersion: v1
kind: Service
metadata:
    name: deyinkaazureaksgitopsplatform
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: deyinkaazureaksgitopsplatform