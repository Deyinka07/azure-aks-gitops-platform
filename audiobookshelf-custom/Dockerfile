# Start with official audiobookshelf image
FROM ghcr.io/advplyr/audiobookshelf:latest

# Metadata about your custom build
LABEL maintainer="deyinkaagboola@gmail.com"
LABEL description="Custom audiobookshelf with CI/CD"
LABEL version="custom-1.0"

# App runs on port 80
EXPOSE 80
